package net.magicmantis.src.model.neuralnetwork;

import java.util.ArrayList;

/**
 * NeuronLayer class: this class has a set of neurons as nodes and receives input from the previous layer and passes
 * to the next layer, resulting in outputs at the final layer.
 */
public class NeuronLayer {

    private int neuronCount;
    private ArrayList<Neuron> neurons;

    public NeuronLayer(int neuronCount) {
        this.neuronCount = neuronCount;
        neurons = new
    }

}
